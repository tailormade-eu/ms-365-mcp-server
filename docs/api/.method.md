# API download method — Microsoft Graph

**Bron:** https://graph.microsoft.com/v1.0/$metadata (OData $metadata, status 200 verified 2026-05-03)
**Docs:** https://learn.microsoft.com/graph/api
**Tool:** OData/EDMX parser → markdown export

## Run

```bash
# Download EDMX metadata
curl -s "https://graph.microsoft.com/v1.0/\$metadata" > docs/api/graph-metadata.xml

# Convert EDMX → markdown (script TBD of via odata2openapi)
node scripts/edmx-to-md.ts docs/api/graph-metadata.xml docs/api/

# Optioneel: leerportaal scrapen voor extra context (rate-limited)
```

## Last-run

- TBD (eerste run nog te doen)
- 2026-05-03: $metadata endpoint verified beschikbaar (HTTP 200)

## Notes

- Graph API heeft groot oppervlak — focus op endpoints die MCP daadwerkelijk wraps
- EDMX = XML schema definitie → makkelijk te parsen
- Auth niet nodig voor metadata
- Beta endpoints: `https://graph.microsoft.com/beta/$metadata` (apart valideren)

## Backlog kandidaten

- B-AUTO-1: Schrijf `scripts/edmx-to-md.ts` (kan ook openapi spec eerst genereren via `odata-openapi`)
- B-AUTO-2: Filter scope tot endpoints die MCP wrapt (anders te groot)
- B-AUTO-3: Beta vs v1.0 verschillen detecteren
